# Bretagne v0.2 — recherche

Bretagne v0.2 est une version de recherche non publique initialisée au Sprint 74 à partir de Bretagne v0.1 publiée et immuable (135 mémoires RX).

Aucun ajout n'est promu au démarrage : candidat courant 135, delta 0.

Le backlog reprend uniquement les dossiers explicitement différés de v0.1 : aviation issue du cycle courant, données ADRASEC publiquement vérifiables, attribution locale des services maritimes déjà présents génériquement dans la base et infrastructures radioamateur nécessitant une revalidation actuelle.

Règles : ne jamais modifier la v0.1 publiée, ne jamais déduire une donnée non publiée, ne jamais dupliquer une fréquence déjà présente pour ajouter une simple attribution locale, et conserver toutes les sorties RadioPack en écoute seule.
